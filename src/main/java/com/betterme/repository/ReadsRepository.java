package com.betterme.repository;

import com.betterme.domain.entity.Reads;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReadsRepository extends JpaRepository<Reads, Long> {
}
